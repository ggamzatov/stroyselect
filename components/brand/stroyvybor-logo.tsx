import { Logo, type LogoProps } from "@/components/brand/logo";

/** @deprecated Use the canonical Logo component in new UI. */
export function StroyVyborLogo(props: LogoProps) {
  return <Logo {...props} />;
}
