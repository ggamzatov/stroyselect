import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import {BadgeCheck,BriefcaseBusiness,Building2,FileCheck2,MapPin,ShieldCheck,Star} from "lucide-react";import {getPublicMarketplaceContractor} from "@/features/contractors/queries/get-public-marketplace-contractor";import {getContractorReviews} from "@/features/reviews/queries/get-contractor-reviews";
type Props={params:Promise<{id:string}>};export async function generateMetadata({params}:Props):Promise<Metadata>{const{id}=await params;const c=await getPublicMarketplaceContractor(id);return c?{title:`${c.publicName} — подрядчик | СтройВыбор`,description:c.description?.slice(0,155)||`Проверенный подрядчик ${c.publicName}`}:{title:"Подрядчик — СтройВыбор"}}
export default async function Page({params}:Props){const{id}=await params;const c=await getPublicMarketplaceContractor(id);if(!c)notFound();const reviews=await getContractorReviews(id);return <main className="min-h-screen bg-background"><div className="app-container py-10"><Link href="/contractors" className="text-sm font-semibold text-muted-foreground">← Все подрядчики</Link><section className="mt-5 rounded-[2rem] border border-border bg-card p-6 md:p-8"><div className="flex gap-4"><div className="flex h-16 w-16 items-center justify-center rounded-2xl bg-secondary text-primary"><Building2/></div><div><div className="flex flex-wrap items-center gap-3"><h1 className="text-3xl font-black">{c.publicName}</h1><span className="inline-flex items-center gap-1 rounded-full bg-emerald-50 px-3 py-1 text-xs font-semibold text-emerald-700"><BadgeCheck className="h-4 w-4"/>Проверен СтройВыбором</span></div><p className="mt-3 text-sm text-muted-foreground"><Star className="mr-1 inline h-4 w-4 text-amber-500"/>{c.rating.toFixed(1)} · {c.ratingCount} отзывов · {c.completedProjectsCount} завершённых проектов</p></div></div>{c.description&&<p className="mt-6 max-w-4xl text-sm leading-7 text-muted-foreground">{c.description}</p>}<Link href="/register" className="mt-6 inline-flex rounded-xl bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground">Создать проект</Link></section>
<div className="mt-6 grid gap-6 xl:grid-cols-[minmax(0,1fr)_360px]"><div className="space-y-6"><Section title="Специализации"><div className="flex flex-wrap gap-2">{c.services.map(s=><span key={s.category_id} className="rounded-full bg-secondary px-3 py-2 text-sm font-semibold">{s.name}</span>)}</div></Section><Section title={`Портфолио (${c.portfolio.length})`}><div className="grid gap-4 md:grid-cols-2">{c.portfolio.map(p=><article key={p.id} className="rounded-2xl border border-border p-5"><BriefcaseBusiness className="h-5 w-5 text-primary"/><h3 className="mt-3 font-bold">{p.title}</h3>{p.city&&<p className="mt-2 text-xs text-muted-foreground"><MapPin className="mr-1 inline h-3 w-3"/>{p.city}</p>}{p.description&&<p className="mt-3 text-sm text-muted-foreground">{p.description}</p>}</article>)}</div></Section><Section title={`Отзывы (${reviews.total})`}><div className="space-y-3">{reviews.reviews.map(r=><article key={r.id} className="rounded-2xl border border-border p-5"><strong>{r.projects?.title??"Завершённый проект"}</strong><span className="float-right font-bold">★ {r.rating}</span>{r.comment&&<p className="mt-3 text-sm text-muted-foreground">{r.comment}</p>}</article>)}</div></Section></div>
<aside className="space-y-6"><Section title="Сведения об исполнителе"><dl className="space-y-3 text-sm"><Row k="Наименование" v={c.legalName||c.publicName}/><Row k="Статус" v={companyType(c.companyType)}/><Row k="ИНН" v={c.inn||"не указан"}/><Row k="ОГРН/ОГРНИП" v={c.ogrn||"не указан"}/></dl><p className="mt-4 text-xs leading-5 text-muted-foreground">Сведения размещены для идентификации исполнителя. Перед заключением договора проверьте реквизиты и разрешения, необходимые для конкретного вида работ.</p></Section><Section title="Trust profile"><div className="space-y-3"><Trust icon={<ShieldCheck className="h-4 w-4"/>} label="Проверка компании" value={c.verifiedAt?"подтверждена":"—"}/><Trust icon={<FileCheck2 className="h-4 w-4"/>} label="Проверенные документы" value={String(c.verifiedDocuments)}/>{c.licenseSummary&&<p className="rounded-xl bg-secondary/50 p-3 text-sm">{c.licenseSummary}</p>}</div></Section><Section title="География"><div className="space-y-2">{c.areas.map((a,i)=><p key={i} className="rounded-xl bg-secondary/50 p-3 text-sm font-semibold"><MapPin className="mr-2 inline h-4 w-4 text-primary"/>{a.city}{a.region?`, ${a.region}`:""}</p>)}</div></Section></aside></div></div></main>}
function Section({title,children}:{title:string;children:React.ReactNode}){return <section className="rounded-[1.75rem] border border-border bg-card p-6"><h2 className="text-xl font-bold">{title}</h2><div className="mt-5">{children}</div></section>}function Row({k,v}:{k:string;v:string}){return <div><dt className="text-xs text-muted-foreground">{k}</dt><dd className="mt-1 font-semibold">{v}</dd></div>}function Trust({icon,label,value}:{icon:React.ReactNode;label:string;value:string}){return <div className="flex gap-3 rounded-xl bg-secondary/45 p-3"><span className="text-primary">{icon}</span><div><p className="text-xs text-muted-foreground">{label}</p><p className="font-semibold">{value}</p></div></div>}function companyType(v:string){return v==="entrepreneur"?"Индивидуальный предприниматель":v==="company"?"Юридическое лицо":v==="self_employed"?"Самозанятый":"Физическое лицо"}
