"use client";
import { useActionState } from "react";
import { addDisputeMessage,openProjectDispute,updateDisputeStatus } from "@/features/workspace/actions/disputes";
type Result={success:boolean;message:string}|null;
type Data=Awaited<ReturnType<typeof import("@/features/workspace/queries/get-project-disputes").getProjectDisputes>>;
export function ProjectDisputes({data}:{data:Data}){const [openState,openAction]=useActionState<Result,FormData>(async(_,f)=>openProjectDispute(f),null);const [messageState,messageAction]=useActionState<Result,FormData>(async(_,f)=>addDisputeMessage(f),null);const [statusState,statusAction]=useActionState<Result,FormData>(async(_,f)=>updateDisputeStatus(f),null);return <main className="min-h-screen bg-background"><div className="app-container py-8"><section className="rounded-[2rem] border bg-card p-6 md:p-8"><p className="text-sm font-semibold text-primary">Disputes & Audit Trail</p><h1 className="mt-2 text-3xl font-black">Споры и журнал действий</h1><p className="mt-2 text-muted-foreground">{data.project.title}</p></section><div className="mt-6 grid gap-6 xl:grid-cols-[minmax(0,1fr)_380px]"><div className="space-y-5">{data.disputes.length===0?<section className="rounded-3xl border bg-card p-6 text-muted-foreground">Открытых и завершённых споров пока нет.</section>:data.disputes.map(d=><article key={d.id} className="rounded-3xl border bg-card p-5"><div className="flex justify-between gap-3"><div><span className="text-xs font-bold uppercase text-primary">{label(d.status)}</span><h2 className="mt-1 text-xl font-black">{d.subject}</h2></div><time className="text-xs text-muted-foreground">{date(d.createdAt)}</time></div><p className="mt-3 whitespace-pre-wrap text-sm">{d.description}</p>{d.resolution&&<div className="mt-4 rounded-xl bg-secondary p-4 text-sm"><b>Итог:</b> {d.resolution}</div>}<div className="mt-5 space-y-2">{d.messages.map(m=><div key={m.id} className={`rounded-xl p-3 text-sm ${m.own?"bg-primary text-primary-foreground":"bg-secondary"}`}><p>{m.body}</p><p className="mt-1 text-[11px] opacity-70">{dateTime(m.createdAt)}</p></div>)}</div>{d.status!=="closed"&&<form action={messageAction} className="mt-4 flex gap-2"><input type="hidden" name="projectId" value={data.project.id}/><input type="hidden" name="disputeId" value={d.id}/><input name="body" required placeholder="Комментарий или уточнение" className="min-h-11 flex-1 rounded-xl border bg-background px-3"/><button className="rounded-xl bg-secondary px-4 font-semibold">Отправить</button></form>}{d.status!=="closed"&&<form action={statusAction} className="mt-3 grid gap-2 sm:grid-cols-[1fr_auto]"><input type="hidden" name="projectId" value={data.project.id}/><input type="hidden" name="disputeId" value={d.id}/><textarea name="resolution" placeholder="Итог решения (обязательно для статуса «Разрешён»)" className="min-h-20 rounded-xl border bg-background p-3"/><div className="flex gap-2 sm:flex-col"><button name="status" value="under_review" className="rounded-xl border px-3 py-2 text-sm">На рассмотрении</button><button name="status" value="resolved" className="rounded-xl bg-primary px-3 py-2 text-sm text-primary-foreground">Разрешить</button><button name="status" value="closed" className="rounded-xl border px-3 py-2 text-sm">Закрыть</button></div></form>}</article>)}<Message state={messageState??statusState}/></div><aside className="space-y-5"><section className="rounded-3xl border bg-card p-5"><h2 className="text-lg font-black">Открыть спор</h2><form action={openAction} className="mt-4 space-y-3"><input type="hidden" name="projectId" value={data.project.id}/><input name="subject" required placeholder="Тема спора" className="min-h-11 w-full rounded-xl border bg-background px-3"/><textarea name="description" required placeholder="Что произошло и какой результат вы ожидаете" className="min-h-28 w-full rounded-xl border bg-background p-3"/><select name="stageId" className="min-h-11 w-full rounded-xl border bg-background px-3"><option value="">Без привязки к этапу</option>{data.stages.map(x=><option key={x.id} value={x.id}>{x.title}</option>)}</select><select name="changeOrderId" className="min-h-11 w-full rounded-xl border bg-background px-3"><option value="">Без change order</option>{data.changeOrders.map(x=><option key={x.id} value={x.id}>{x.title}</option>)}</select><select name="paymentId" className="min-h-11 w-full rounded-xl border bg-background px-3"><option value="">Без платежа</option>{data.payments.map(x=><option key={x.id} value={x.id}>{x.amount.toLocaleString("ru-RU")} ₽ · {x.paidAt}</option>)}</select><button className="min-h-11 w-full rounded-xl bg-primary font-semibold text-primary-foreground">Открыть спор</button></form><Message state={openState}/></section><section className="rounded-3xl border bg-card p-5"><h2 className="text-lg font-black">Audit trail</h2><p className="mt-1 text-xs text-muted-foreground">Append-only журнал: записи нельзя изменить или удалить.</p><div className="mt-4 max-h-[520px] space-y-3 overflow-auto">{data.audit.length===0?<p className="text-sm text-muted-foreground">Событий пока нет.</p>:data.audit.map(a=><div key={a.id} className="border-l-2 border-primary/30 pl-3"><p className="text-sm font-semibold">{auditLabel(a.action)}</p><p className="text-xs text-muted-foreground">{a.entity_type}{a.entity_id?` · ${a.entity_id.slice(0,8)}`:""}</p><p className="text-[11px] text-muted-foreground">{dateTime(a.createdAt)}</p></div>)}</div></section></aside></div></div></main>}
function Message({state}:{state:Result}){return state?<p className={`mt-3 rounded-xl p-3 text-sm ${state.success?"bg-emerald-50 text-emerald-800":"bg-red-50 text-red-800"}`}>{state.message}</p>:null}function label(v:string){return({open:"Открыт",under_review:"На рассмотрении",resolved:"Разрешён",closed:"Закрыт"} as Record<string,string>)[v]??v}function auditLabel(v:string){return({dispute_opened:"Открыт спор",dispute_message_added:"Добавлено сообщение",dispute_status_changed:"Изменён статус спора"} as Record<string,string>)[v]??v}function date(v:string){return new Intl.DateTimeFormat("ru-RU",{dateStyle:"medium"}).format(new Date(v))}function dateTime(v:string){return new Intl.DateTimeFormat("ru-RU",{dateStyle:"short",timeStyle:"short"}).format(new Date(v))}
