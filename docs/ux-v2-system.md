# StroySelect UX v2 — final system

The interface is organized around the user's next task, with progressive disclosure of secondary information. Primary actions use the brand primary color, cards use consistent elevation, and touch targets are designed for comfortable mobile use.

The system also respects `prefers-reduced-motion` so decorative and interaction transitions do not create unnecessary motion for users who disable it.

This document is non-runtime documentation.
